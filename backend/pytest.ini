[pytest]
addopts = -ra --strict-markers
markers =
    integration: requires the isolated PostgreSQL test database
    performance: query-budget and performance checks
    workflow: complete ERP workflow scenarios
filterwarnings =
    error::RuntimeWarning
